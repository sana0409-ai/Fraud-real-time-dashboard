## Packages
recharts | Data visualization for KPIs and charts
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
WebSocket connects to /ws path for real-time updates (kpis and recent events).
Dark mode dashboard aesthetic is required.
Icons will use Lucide React (already in stack).
Form handling for the prediction tester.
